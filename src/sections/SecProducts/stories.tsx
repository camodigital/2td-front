import { Story, Meta } from '@storybook/react/types-6-0'
import SecProducts from '.'

export default {
  title: 'SecProducts',
  component: SecProducts
} as Meta

export const Default: Story = () => <SecProducts />
