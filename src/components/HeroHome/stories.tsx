import { Story, Meta } from '@storybook/react/types-6-0'
import HeroHome from '.'

export default {
  title: 'HeroHome',
  component: HeroHome
} as Meta

export const Default: Story = () => <HeroHome />
