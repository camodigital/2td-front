import { Story, Meta } from '@storybook/react/types-6-0'
import NavLinks from '.'

export default {
  title: 'NavLinks',
  component: NavLinks
} as Meta

export const Default: Story = () => <NavLinks />
